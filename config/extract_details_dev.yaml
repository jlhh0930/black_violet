default:
  extracts:
    terminated_customers:
    transactions:

clients:
  client_one:
    sftp:
      dev: dev_sftp_bucket_one
  client_two:
    sftp:
      dev: dev_sftp_bucket_two

states:
  al:
    extracts: reconciled_report
    kms: kms-key
    sftp: state_sftp_bucket_al
  ak:
    extracts:
    kms: kms-key
    sftp: state_sftp_bucket_ak